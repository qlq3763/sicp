;#lang racket
;(require (planet "sicp.ss" ("soegaard" "sicp.plt" 2 1)))

;(require (only (lib "etc.ss")
;               this-expression-source-directory))
;(require (lib "etc.ss"))


;(define fovnder 
;  (load-painter 
;   (build-path (this-expression-source-directory)
;               "fovnder.gif")))

(define fovnder (load-painter "fovnder.gif"))